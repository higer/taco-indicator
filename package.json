{
  "name": "taco-indicator",
  "version": "1.0.0",
  "description": "TACO Indicator - Trump Always Chickens Out - Market Stress Composite Index",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "fetch": "node -e \"require('dotenv').config();require('./fetcher').fetchAllData().then(()=>console.log('Done')).catch(console.error)\""
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "node-cron": "^3.0.3",
    "node-fetch": "^2.7.0"
  }
}
